import type { Experience } from "@/types";

/**
 * Work / internship experience. Populated as roles are taken on; the timeline
 * and any experience UI derive from this list without code changes (AP-5).
 */
export const experience: Experience[] = [];
