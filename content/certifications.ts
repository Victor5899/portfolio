import type { Certification } from "@/types";

/**
 * Certifications (SRS §7.8). Add entries here as they are earned — the
 * Certifications section renders whatever is present and hides itself when empty.
 */
export const certifications: Certification[] = [];
