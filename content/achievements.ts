import type { Achievement } from "@/types";

/**
 * Achievements (SRS §7.7). Add entries here; the Achievements section renders
 * whatever is present and hides itself when empty.
 */
export const achievements: Achievement[] = [];
